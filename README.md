# Advent of Code 2025

## Preparing the environment

This project is integrated with `uv`. Use `uv sync` to install the dependencies.

## Running

Use `uv run pytest` to run the unit tests.

## Profiling

Use `uv run pytest --profile`.

